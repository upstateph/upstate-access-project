[pytest]
# Scope collection to the engine suite. Without testpaths, a bare `pytest` from the
# repo root walks everything — including outreach/, the in-place clone of the private
# outreach repo.
testpaths = engine/tests
norecursedirs = outreach .venv data dist deploy/lite node_modules
