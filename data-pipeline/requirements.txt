# Data pipeline dependencies (Phase 0)
requests>=2.31
pandas>=2.0

# Added in later phases (kept here for reference; uncomment when needed):
# gtfs-kit>=6.0      # Greenlink GTFS parsing (Phase 2)
# partridge>=1.1     # alternative GTFS reader (Phase 2)
